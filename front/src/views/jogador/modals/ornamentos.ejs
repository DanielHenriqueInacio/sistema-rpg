<%- contentFor("estilo") %>

<%- contentFor("content") %>

<div class="container">
    <div class="row">
        <div class="col-md-12">

            <div class="row mt-5">
                <div class="col-md-12">
                    <h1 class="h2 mb-3 fw-normal text-center">Ornamentos</h1>
                </div>
                <div class="col-md-12">
                    <form action="">
                        <table class="table table-bordered text-center">
                            <thead>
                                <tr>
                                    <th>Selecione</th>
                                    <th>Nome</th>
                                    <th>Imagem</th>
                                    <th>Descrição</th>
                                </tr>
                            </thead>
                            <tbody>
                            <% for(const ornamento of ornamentos.data) { %>
                                <tr>
                                    <td value="" class="col-md-2">
                                        <div class="form-check form-check-inline px-5">
                                            <input class="form-check-input" type="radio" name="exampleRadios"
                                                   id="exampleRadios2" value="option2">

                                        </div>
                                    </td>
                                    <td value="<%= ornamento.id %>"
                                        class="col-md-3"><%= ornamento.attributes.nome %></td>
                                    <td value="<%= ornamento.id %>"
                                        class="col-md-3"><%= ornamento.attributes.imagem %></td>
                                    <td value="<%= ornamento.id %>"
                                        class="col-md-4"><%= ornamento.attributes.descricao %></td>
                                </tr>
                            <% } %>
                            </tbody>
                        </table>
                    </form>
                </div>
            </div>

            <button type="submit" class="btn btn-primary">Salvar</button>
        </div>
    </div>
</div>